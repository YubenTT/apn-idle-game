# Vendor surface

Intentionally empty. The render-lock uses WebGL2 and a local GLB parser instead
of a third-party runtime. This directory is reserved for a pinned renderer only
if a future superseding ADR requires one.
